---
vault_container_name: "ansible-vault-dev"
postgresql_container_name: "ansible-vault-postgres"
container_compose_runtime: "{{ lookup('env', 'VAULT_COMPOSE_CONTAINER_RUNTIME') | default('docker compose', true) }}"